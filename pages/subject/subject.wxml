<!--pages/subject/subject.wxml-->
<view class='wrap' wx:for="{{list}}" wx:key="{{index}}" data-type="{{item.type}}" bindtap='subjectTap'>
  <image mode='widthFix' src="{{item.url}}"></image>
  <text>{{item.type}}</text>
</view>
